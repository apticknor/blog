<!doctype html>
<html lang="en-us">
    <head>
        <!-- META DATA -->
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <!-- SEO -->
        <title>{{ title }} | {{ site.title }}</title>
        <meta name="description" content="{{#if excerpt}}{{ excerpt }}{{else}}{{ site.description }}{{/if}}" />
        <link rel="canonical" href="{{ permalink }}" />
        <!-- STYLESHEETS -->
        <link rel="stylesheet" media="screen and (min-width: 1em)" href="{{ site.url }}/assets/styles/app.css" />
{{!--
        <!-- SCRIPTS -->
        <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
        <script src="{{ site.url }}/assets/scripts/app.min.js"></script>
--}}
    </head>
    <body>

        <div class="tier">
            <div class="wrapper">
                <div class="masthead" role="banner">
                    <div class="masthead-logo">
                        <a href="/" class="logo">
                            <h1>
                                 {{ site.title }}
                            </h1>
                        </a>
                    </div>
                    <div class="masthead-nav" role="navigation">
                        <ul class="masthead-nav-list">
                            <!--
                            {{#each primaryNav}}
                            <li>
                                <a href="{{ this.link }}">
                                    {{ this.label }}
                                </a>
                            </li>
                            {{/each}}
                            -->
                        </ul>
                    </div>
                </div>
            </div>
        </div>
